import styled from "styled-components";

const StyledMovieLengthYear = styled.div`
  font-size: 11px;
  color: rgba(255, 255, 255, 0.5);
  animation-delay: 0.3s;
`;

export default StyledMovieLengthYear;
